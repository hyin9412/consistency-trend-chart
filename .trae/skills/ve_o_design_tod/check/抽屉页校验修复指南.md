# ve-o 抽屉页校验与修复指南（文档版）

本文档用于排查"抽屉页"常见不符合设计规则的问题。用于页面生成后的对照检查与修复，不依赖脚本或命令。

## 适用范围

- 抽屉页
- 抽屉页标题区与内容区布局
- 抽屉页与内容载体组件（表格、基础信息等）

## 使用方法

- 按"规则清单"逐条核对页面表现（建议同时开浏览器 DevTools 观察 DOM 与样式）。
- 若命中问题，按对应"修复步骤"修改代码与样式。
- 修复后再次回归验证（页面刷新、打开/关闭抽屉、改变窗口宽度、再次进入页面）。

## 规则清单
